// imports
import { BASE_URL, API_KEY } from './utils';
import axios from 'axios';
import { showLoader, hideLoader, showNotification } from './loader';

export const displayMovies = moviesId => {
  console.log(moviesId);
};
